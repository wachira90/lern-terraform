# tfplan

คำสั่ง `terraform plan -out=plan.tfplan` คือคำสั่งสำหรับ **ดูตัวอย่างการเปลี่ยนแปลงและบันทึกแผนการทำงาน (Execution Plan)** ของ Terraform เอาไว้ในรูปแบบไฟล์

เราสามารถแยกส่วนอธิบายคำสั่งได้ดังนี้:

* `terraform plan` : เป็นการสั่งให้ Terraform ตรวจสอบโค้ด (Configuration) เทียบกับสถานะปัจจุบัน (State) เพื่อคำนวณว่ามี Infrastructure ใดบ้างที่กำลังจะถูก **สร้าง (Create)**, **แก้ไข (Update)**, หรือ **ลบ (Destroy)** ซึ่งขั้นตอนนี้จะยังไม่มีการเปลี่ยนแปลงเกิดขึ้นจริง (เป็นเพียงการ Preview)
* `-out=plan.tfplan` : เป็น Option (Flag) ที่ระบุว่าให้บันทึกแผนการเปลี่ยนแปลงที่คำนวณได้เมื่อสักครู่นี้ ลงไปในไฟล์ที่ชื่อว่า `plan.tfplan`

---

### ข้อดีและประโยชน์ของการใช้คำสั่งนี้

* **ป้องกันความผิดพลาด (Safety & Predictability):** เมื่อคุณตรวจสอบแผนเสร็จแล้ว คุณสามารถนำไฟล์นี้ไปรันรันสร้างจริงได้ด้วยคำสั่ง `terraform apply plan.tfplan` ซึ่ง Terraform จะทำตามที่บันทึกไว้ในไฟล์นี้เป๊ะๆ ช่วยป้องกันปัญหาที่อาจเกิดการเปลี่ยนแปลงของระบบ (State) หรือมีคนไปแก้โค้ดแทรกกลางคันระหว่างช่วงเวลาที่คุณกำลัง `plan` และกำลังจะกด `apply`
* **ใช้ในระบบอัตโนมัติ (CI/CD Pipelines):** เป็นท่ามาตรฐานที่ใช้ในระบบ CI/CD (เช่น GitHub Actions, Jenkins, GitLab CI) โดยระบบจะทำการสร้างไฟล์ `plan.tfplan` ขึ้นมาเพื่อให้ทีมงานรีวิว (Approve) ก่อน เมื่อได้รับอนุมัติแล้ว ระบบถึงจะนำไฟล์นั้นไป `apply` ต่อไป

> **ข้อควรระวัง:** ไฟล์ `.tfplan` อาจมีข้อมูลที่มีความละเอียดอ่อน (Sensitive Data) เช่น รหัสผ่าน หรือ API Keys รวมอยู่ด้วย ไม่ควรนำไฟล์นี้ไป Commit ลงใน Git หรือแชร์ในที่สาธารณะ
